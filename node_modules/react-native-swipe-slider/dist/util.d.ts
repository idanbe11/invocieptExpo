import { PanResponderGestureState } from 'react-native';
export declare type Dimensions = {
    width: number;
    height: number;
};
export declare const calculateValue: (gestureState: PanResponderGestureState, dimensions: Dimensions, min: number, max: number, step: number, vertical: boolean, prevValue: number) => number;
