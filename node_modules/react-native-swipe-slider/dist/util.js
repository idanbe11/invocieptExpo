"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calculateValue = void 0;
const calculateValue = (gestureState, dimensions, min, max, step, vertical, prevValue) => {
    const ratio = vertical
        ? dimensions.height
            ? -gestureState.dy / dimensions.height
            : 0
        : dimensions.width
            ? gestureState.dx / dimensions.width
            : 0;
    const diff = max - min;
    return Math.max(min, Math.min(max, prevValue + Math.round((ratio * diff) / step) * step));
};
exports.calculateValue = calculateValue;
//# sourceMappingURL=util.js.map